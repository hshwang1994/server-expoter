# server-exporter — 테스트 / 검증용 Python 의존성
#
# 적용 대상:
#   - tests/* (pytest, baseline 검증)
#   - tests/e2e_browser/* (Playwright Browser E2E)
#   - tests/redfish-probe/* (실장비 probe)
#
# 작성: 2026-04-29 cycle-014 (실장비 권한 부여 + Playwright 도입)
#
# 운영 Agent (10.100.64.154/155)는 ansible-core / pywinrm / pyvmomi 등을
# OS 패키지/시스템 venv로 관리. 본 파일은 개발 / Browser E2E 머신용.

# pytest (이미 사용 중인 8개 테스트 호환)
pytest>=8.0
pytest-asyncio>=0.23

# Browser E2E
playwright>=1.46
pytest-playwright>=0.5

# Redfish / SSH / WinRM probe (실장비)
# Redfish는 stdlib (urllib/ssl/json) — 외부 의존 없음 (rule 10 R2)
paramiko>=3.4         # Linux SSH probe (password 인증 지원)
pywinrm>=0.5          # Windows WinRM probe (Win10 / Server 2022)
requests>=2.32        # 일반 HTTP probe (Browser 외 시나리오)

# 개발 편의
ruff>=0.5             # lint
black>=24             # format
