---
# ── Origin metadata (rule 96 R1) ──────────────────────────────────────────────
# Vendor: VMware ESXi 6.x
# Firmware: tested_against ["6.0", "6.5", "6.7"]
# Origin: 추정 — 검증 필요 (community.vmware 6.2.0 + vSphere 6.x release notes)
# Last sync: 2026-04-27 (hshwang1994)
# Notes: ESXi 6.x는 EOL. 잔존 환경 대비 priority=30 (7.x=50/8.x=100보다 낮음).
# ──────────────────────────────────────────────────────────────────────────────
adapter_id: esxi_6x
channel: esxi
priority: 30

match:
  version_patterns: ["^6\\."]

capabilities:
  sections_supported:
    - system
    - hardware
    - cpu
    - memory
    - storage
    - network

collect:
  collect_tasks:
    - "esxi-gather/tasks/collect_facts.yml"
    - "esxi-gather/tasks/collect_config.yml"
    - "esxi-gather/tasks/collect_datastores.yml"
  normalize_tasks:
    - "esxi-gather/tasks/normalize_system.yml"
    - "esxi-gather/tasks/normalize_network.yml"
    - "esxi-gather/tasks/normalize_storage.yml"

credentials:
  profile: "esxi_default"
  fallback_profiles: []

graceful_degradation:
  on_section_fail: continue
  on_auth_fail: abort
  critical_sections: [system, hardware]
  optional_sections: [storage]
