# F37 — Linux IB 도구 부재 graceful (검증만)

**우선**: P3 (F07/F23 묶음) / **영역**: hba_ib (호환성) / **출처**: R6 / **원칙**: Additive

## 컨텍스트
Linux InfiniBand 환경에서 `ibstat` / `ibv_devinfo` / `mlxconfig` 미설치 가능 (rdma-core / libibverbs / mft 패키지 부재).

## 현재 동작
`os-gather/tasks/linux/gather_hba_ib.yml`:
- lspci 기반 식별 (graceful failed_when:false)
- 도구 부재 시 빈 결과

## 변경 (Additive)
F07 / F23 묶음 — cycle 2026-05-01 인프라 (`_sections_unsupported_fragment`) 활용:
```yaml
- ansible.builtin.shell: command -v lspci
  register: _l_lspci_check

- ansible.builtin.set_fact:
    _sections_unsupported_fragment: ['hba_ib']
  when: _l_lspci_check.rc != 0
```

## 회귀 risk
없음 (Additive — graceful).

## 사용자 의도

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 은 cycle DONE 시점에 cold-start 6 절 정본 도입 전 작성. 원본 의도는 title + 머리말 메타 + cycle CURRENT_STATE entry 참조.)

## 결정 / 결과

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 의 결정 / 결과 는 commit log + cycle CURRENT_STATE entry + INDEX.md ticket 상태 참조. cycle DONE 시점에 cold-start 6 절 정본 도입 전 작성.)

## 다음 지시 / 관련

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 의 다음 지시 / 관련 ticket 은 cycle INDEX.md / DEPENDENCIES.md 참조.)
