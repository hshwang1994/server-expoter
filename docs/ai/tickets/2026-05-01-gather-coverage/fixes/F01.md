# F01 — SystemType=DPU enum 통과 검증

**우선**: P3 / **영역**: system / **출처**: R1 DMTF / **원칙**: Additive

## 컨텍스트
Redfish 2024.1 SystemType 신규 enum 'DPU' (NVIDIA SmartNIC / BlueField).

## 현재 동작
`_safe(data, 'SystemType')` raw read — 'Physical'/'Virtual'/'DPU' 모두 string 통과. 분기 코드 없음. **이미 호환** (string raw 그대로 emit).

## 변경 (Additive)
없음. 코드 수정 불필요.

## 검증
NVIDIA BlueField DPU host fixture 추가 후 envelope `data.hardware.system_type='DPU'` 확인.

## Cold-start
1. NVIDIA BlueField BMC ServiceRoot 응답 확보
2. `tests/fixtures/redfish/nvidia_dpu/` 추가
3. probe_redfish 결과 확인 — system 섹션 success / system_type='DPU'

## 회귀 risk
없음 (변경 없음).

## 사용자 의도

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 은 cycle DONE 시점에 cold-start 6 절 정본 도입 전 작성. 원본 의도는 title + 머리말 메타 + cycle CURRENT_STATE entry 참조.)

## 결정 / 결과

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 의 결정 / 결과 는 commit log + cycle CURRENT_STATE entry + INDEX.md ticket 상태 참조. cycle DONE 시점에 cold-start 6 절 정본 도입 전 작성.)
