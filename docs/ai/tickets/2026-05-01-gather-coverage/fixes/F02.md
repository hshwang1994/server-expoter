# F02 — ProcessorType 'Accelerator'/'Core' 통과

**우선**: P2 / **영역**: cpu / **출처**: R1 DMTF / **원칙**: Additive

## 컨텍스트
Redfish 2024.x ProcessorType enum: CPU / GPU / FPGA / DSP / **Accelerator** / **Core** (신규 'Core' 는 logical core 단위).

## 현재 동작
`redfish-gather/tasks/normalize_standard.yml:25-32`:
```yaml
{%- if ptype == 'CPU' or ptype == '' or ptype is none -%}
```
'Accelerator' / 'Core' 모두 제외 (GPU와 묶임).

## 변경 (Additive)
```yaml
{%- if ptype in ('CPU', 'CORE', '') or ptype is none -%}
```
- 대문자 정규화 후 비교
- 'Core' 추가 (logical processor 합산 의도)
- GPU / FPGA / DSP / Accelerator 여전히 제외 (cycle 2026-04-29 fix B01 의도 유지)

## 회귀
- lab Dell R760 Tesla T4 fixture: GPU 제외 유지 검증
- 신규 'Core' 응답 fixture 필요 (deep CPU detail 응답 BMC)

## Cold-start
1. fixture 추가 후 normalize_standard.yml 한 줄 수정
2. pytest 회귀 — 기존 cpu summary 변화 없음 검증

## 회귀 risk
'Core' 응답 BMC가 'CPU'와 'Core' 양쪽 emit 시 합산 중복 가능 — fixture 검증 필수.
