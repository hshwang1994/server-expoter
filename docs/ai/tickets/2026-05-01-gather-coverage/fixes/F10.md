# F10 — HPE Gen11 HBM memory enum 검증

**우선**: P2 / **영역**: memory / **출처**: R2 / **원칙**: Additive (현재 코드 호환)

## 컨텍스트
HPE ProLiant Gen11 SuperDome / accelerator 시스템 HBM (High Bandwidth Memory) 모듈 추가. MemoryType / BaseModuleType 'HBM' 또는 'HBM2'/'HBM3' enum 응답.

## 현재 동작
`redfish_gather.py:1134` `gather_memory`:
- `MemoryType` raw read (string)
- `BaseModuleType` raw read

`normalize_standard.yml:250-272` `_rf_summary_memory`:
- BaseModuleType / MemoryType / Manufacturer 별로 그룹 자동 분리

→ HBM enum 응답 시 자동 별도 그룹 생성 (변경 없음)

## 변경
없음 (코드 자동 호환).

## 검증
HPE Gen11 Compute (HBM) host 응답 fixture 확보 후:
1. probe_redfish 수집
2. envelope `data.memory.summary.groups` 에 HBM 그룹 분리 확인
3. baseline 갱신 (HBM 모델일 시)

## Cold-start
1. lab HPE Gen11 SuperDome / accelerator 호스트 접근
2. fixture 캡처
3. 검증

## 회귀 risk
없음.
