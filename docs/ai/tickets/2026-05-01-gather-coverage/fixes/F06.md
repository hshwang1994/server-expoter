# F06 — thermal 섹션 신규 도입

**[STATUS]**: ⚠️ **호환성 영역 외 — 신규 섹션** (사용자 의도 2026-05-01 명시: "호환성 fallback only"). 별도 cycle.

**우선**: P2 (사용자 승인 후) / **영역**: thermal (신규) / **출처**: R1 DMTF / **원칙**: Additive (단 신규 섹션)

## 컨텍스트
DMTF 2020.4 ThermalSubsystem (구 Thermal deprecated). 우리 코드 미수집.

## 변경 (Additive — 신규 섹션 7단계)

1. **schema/sections.yml** — `thermal` section entry 추가
2. **schema/field_dictionary.yml** — thermal 필드 정의:
   - `data.thermal.temperatures[]`: name / reading_celsius / threshold_critical / status
   - `data.thermal.fans[]`: name / reading_rpm / reading_percent / status
   - `data.thermal.summary`: temp_critical_count / fan_critical_count
3. **`redfish_gather.py` `gather_thermal()` 신규 함수**:
   ```python
   def gather_thermal(bmc_ip, chassis_uri, username, password, timeout, verify_ssl):
       errors = []
       if not chassis_uri: return {}, []
       # 구 path
       path = _p(chassis_uri) + '/Thermal'
       st, data, err = _get(...)
       if st == 404:
           # 신 path fallback (DMTF 2020.4)
           return _gather_thermal_subsystem(...)
       if perr or st != 200:
           return {}, []   # 미지원 (cycle 2026-05-01 패턴)
       # Temperatures + Fans 매핑 ...
   ```
4. **`_collect_all_sections`** 에 'thermal' dispatch 추가
5. **adapter 16개 capabilities `sections_supported`** 에 'thermal' 추가
6. **baseline 7 vendor json** 에 thermal section 추가
7. **normalize_standard.yml** 에 thermal fragment

## 회귀
- 7 vendor baseline 변경 — schema 버전 변경 (rule 92 R5 사용자 명시 승인 필수)
- 기존 9 섹션 동작 유지 — fragment 철학 (rule 22)
- PowerSubsystem fallback 패턴 적용 (cycle 2026-05-01 인프라)

## Cold-start
1. schema 변경 사용자 승인 — rule 92 R5
2. 7단계 순서대로 진행 (rule 22 R2)
3. 회귀 fixture: lab vendor 별 Thermal + ThermalSubsystem 응답
4. Jenkins Stage 3 (validate schema) 통과

## 회귀 risk
- 사용자 승인 없으면 진행 금지 (rule 92 R5)
- baseline 7개 동시 갱신 — 잘못 갱신 시 회귀 무력화

## 우선순위 변경 가능
사용자 결정 필요 — "thermal 정보 운영상 가치" 기준

## 사용자 의도

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 은 cycle DONE 시점에 cold-start 6 절 정본 도입 전 작성. 원본 의도는 title + 머리말 메타 + cycle CURRENT_STATE entry 참조.)

## 결정 / 결과

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 의 결정 / 결과 는 commit log + cycle CURRENT_STATE entry + INDEX.md ticket 상태 참조. cycle DONE 시점에 cold-start 6 절 정본 도입 전 작성.)
