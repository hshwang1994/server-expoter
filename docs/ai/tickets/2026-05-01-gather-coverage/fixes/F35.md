# F35 — Manager URI 다양한 위치 (검증만)

**우선**: P3 / **영역**: bmc / **출처**: R5 / **원칙**: 이미 호환

## 컨텍스트
Manager URI 변종 (vendor 별):
- HPE: `/redfish/v1/Managers/1`
- OpenBMC: `/redfish/v1/bmc`
- HPE Superdome: `/redfish/v1/Managers/RMC`
- Cisco: `/redfish/v1/Managers/CIMC`
- Lenovo XCC: `/redfish/v1/Managers/1`
- Dell iDRAC: `/redfish/v1/Managers/iDRAC.Embedded.1`
- Supermicro: `/redfish/v1/Managers/1`

## 현재 동작
`redfish_gather.py:589` `detect_vendor`:
```python
manager_uri, _, _ = _resolve_first_member_uri(
    bmc_ip, _safe(root, 'Managers', '@odata.id'),
    username, password, timeout, verify_ssl,
)
```

→ ServiceRoot.Managers collection의 첫 member URI 동적 사용. **모든 vendor URI 변종 자동 호환**.

## 변경
없음. 이미 호환.

## 회귀 risk
없음.

## 사용자 의도

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 은 cycle DONE 시점에 cold-start 6 절 정본 도입 전 작성. 원본 의도는 title + 머리말 메타 + cycle CURRENT_STATE entry 참조.)

## 결정 / 결과

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 의 결정 / 결과 는 commit log + cycle CURRENT_STATE entry + INDEX.md ticket 상태 참조. cycle DONE 시점에 cold-start 6 절 정본 도입 전 작성.)

## 다음 지시 / 관련

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 의 다음 지시 / 관련 ticket 은 cycle INDEX.md / DEPENDENCIES.md 참조.)
