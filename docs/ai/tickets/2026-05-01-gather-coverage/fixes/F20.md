# F20 — backoff 1초 → 5초 (BMC lockout 회피)

**우선**: P2 / **영역**: users / **출처**: R3 / **원칙**: Additive (단순 변경)

## 컨텍스트
BMC vendor lockout policy:
- Dell iDRAC: 5회 fail / 5분 lockout
- HPE iLO: 3회 fail (펌웨어별)
- Lenovo XCC: 5회 fail / 사용자 설정

cycle 2026-04-30 try_one_account.yml backoff 1초 → multi-account 시도 시 lockout risk.

## 현재 동작
`redfish-gather/tasks/try_one_account.yml:70`:
```yaml
- name: "redfish | try_account | backoff on failure"
  ansible.builtin.command: sleep 1
```

## 변경
```yaml
- name: "redfish | try_account | backoff on failure"
  ansible.builtin.command: sleep 5
```

## 영향
- multi-account vault (5개 자격) × 5초 = 25초 추가 / host
- Jenkins gather stage timeout 영향 검토 (현재 60min — 영향 적음)
- forks=200 동시 진행 영향 — agent 부하 증가 감안

## 회귀
- 빌드 시간 약간 증가 (multi-account 호스트만)
- BMC lockout 사고 감소

## Cold-start
1. try_one_account.yml 한 줄 수정
2. Jenkins gather stage timeout 검토
3. 사이트 빌드 시간 모니터링

## 회귀 risk
큰 inventory + multi-account vault 시 빌드 시간 증가 — 운영 영향 평가

## 사용자 의도

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 은 cycle DONE 시점에 cold-start 6 절 정본 도입 전 작성. 원본 의도는 title + 머리말 메타 + cycle CURRENT_STATE entry 참조.)

## 결정 / 결과

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 의 결정 / 결과 는 commit log + cycle CURRENT_STATE entry + INDEX.md ticket 상태 참조. cycle DONE 시점에 cold-start 6 절 정본 도입 전 작성.)
