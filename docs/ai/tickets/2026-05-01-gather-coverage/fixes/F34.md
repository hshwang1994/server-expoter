# F34 — Drive Protocol OEM enum 자동 통과 (검증만)

**우선**: P3 / **영역**: storage / **출처**: R5 / **원칙**: 이미 호환

## 컨텍스트
Redfish Drive.Protocol enum: AHCI / iSCSI / SAS / SATA / NVMe / PCIe / FC / FCoE / FCP / FICON / NFS / SMB / **NVMe-oF** (신) / **SAS-4** (신) + OEM 확장.

## 현재 동작
`redfish_gather.py:1180+` (storage 안):
```python
'protocol': drv.protocol|default(none),
```

`_safe(drv, 'Protocol')` raw read — 모든 enum 자동 string 통과.

## 변경
없음. 이미 호환.

## 검증 (추후 lab/사이트)
- NVMe-oF SSD 환경: `data.storage.physical_disks[*].protocol == 'NVMe-oF'` emit 확인
- SAS-4 컨트롤러: protocol == 'SAS-4' emit 확인
- OEM 확장: 그대로 emit

## 회귀 risk
없음 (변경 없음).

## 사용자 의도

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 은 cycle DONE 시점에 cold-start 6 절 정본 도입 전 작성. 원본 의도는 title + 머리말 메타 + cycle CURRENT_STATE entry 참조.)

## 결정 / 결과

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 의 결정 / 결과 는 commit log + cycle CURRENT_STATE entry + INDEX.md ticket 상태 참조. cycle DONE 시점에 cold-start 6 절 정본 도입 전 작성.)

## 다음 지시 / 관련

(cycle 2026-05-11 Phase 7 추가 stub — 본 ticket 의 다음 지시 / 관련 ticket 은 cycle INDEX.md / DEPENDENCIES.md 참조.)
