# F04 — HPE iLO 5 BaseNetworkAdapters fallback (Additive)

**우선**: P2 / **영역**: network_adapters / **출처**: R1+R2 / **원칙**: Additive (vendor 분기 OEM allowed)

## 컨텍스트
HPE iLO 5 초기 펌웨어 (2.x 초기) 가 표준 `Chassis/{id}/NetworkAdapters` 응답 안 함, OEM `/Systems/1/BaseNetworkAdapters` 만. iLO 5 1.10+ 부터 표준 NA 통합.

## 현재 동작
`redfish_gather.py:1481` `gather_network_adapters_chassis`:
- `Chassis/{id}/NetworkAdapters` 만 시도
- 404 시 cycle 2026-05-01 fix로 'not_supported' 분류 — noise 차단 OK
- BUT HPE 구 펌웨어는 **데이터 손실** (OEM path 미수집)

## 변경 (Additive)
```python
def gather_network_adapters_chassis(bmc_ip, chassis_uri, ...):
    base = _p(chassis_uri) + '/NetworkAdapters'
    st, coll, err = _get(...)
    if st == 200:
        # 기존 표준 path 처리 (변경 없음)
        ...
    elif st == 404 and detected_vendor == 'hpe':  # nosec rule12-r1 (OEM 영역)
        # HPE iLO 5 구 펌웨어 OEM fallback
        st_oem, coll_oem, _ = _get(bmc_ip, 'Systems/1/BaseNetworkAdapters', ...)
        if st_oem == 200:
            return _parse_hpe_base_na(coll_oem, ...), []
        return out, []   # 미지원 — cycle 2026-05-01 패턴
    return out, []   # 미지원 (다른 vendor)
```

신규 함수 `_parse_hpe_base_na()`: HPE OEM schema → 표준 NetworkAdapter 호환 dict 매핑.

## 회귀
- lab Dell / Cisco / Lenovo / Supermicro: 표준 path 정상 → 동작 유지
- HPE Gen11 / Gen12 (iLO 6 / 신 iLO 5): 표준 path 200 → OEM 미사용 (변경 없음)
- HPE iLO 5 구 펌웨어: 표준 404 → OEM fallback 시도

## Cold-start
1. HPE iLO 5 구 펌웨어 (2.x 초기) ServiceRoot + BaseNetworkAdapters 응답 fixture 캡처
2. `_parse_hpe_base_na()` 작성 (HPE OEM schema 매핑)
3. `gather_network_adapters_chassis` 수정 (vendor 분기 — rule 12 R1 Allowed: OEM)
4. pytest unit (test_hpe_base_na_fallback)
5. 회귀 — Gen11/12 표준 path 영향 없음 검증

## 회귀 risk
HPE 신 펌웨어가 우연히 standard 404 (다른 이유) 시 OEM fallback 실행 — OEM 응답이 비어 있을 가능성. graceful degradation으로 noise 안 만듦.
