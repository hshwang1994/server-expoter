# F44 — Huawei iBMC adapter 신규 (vault 미생성)

**우선**: P1 / **영역**: 신 vendor / **출처**: 7-loop audit Loop 6 (audit ID F74) / **원칙**: Additive

## 사용자 명시 (2026-05-01)

> "신규 장비 도입할 의향이 있다 다만 테스트할 lab 장비가 없다. 일단 vault는 만들지 말고 코드 생성에 대한 티켓은 만들어라"

→ rule 50 R2 (vendor 추가 9단계) 중 **vault 단계 SKIP**. 향후 lab 또는 사이트 장비 도입 시 vault 만 추가하면 운영 가능.

## 컨텍스트

- 대상 vendor: **Huawei** (FusionServer Pro V5 / V6 시리즈)
- BMC 이름: **iBMC** (integrated Baseboard Management Controller)
- Redfish 지원: 표준 Redfish + IPMI 2.0 + SNMP + DCMI
- 대표 모델: 2288H V5 / 2288X V5 / 2488H V5 / 1288H V6
- web sources: rule 96 R1-A 충족 (공식 Huawei docs 다수)

## web sources (rule 96 R1-A origin)

- [Huawei iBMC FusionServer Pro 2288H V5 User Guide](https://support.huawei.com/enterprise/en/doc/EDOC1000163550/41dfb258/ibmc) — 공식
- [FusionServer Pro V5/V6 Datasheet](https://networktelecom.ru/images/data/gallery/3071_9834_Huawei-FusionServer-Pro-V5-Rack-Server-Data-Sheet.pdf)
- [Huawei-iBMC-Cmdlets (GitHub)](https://github.com/Huawei/Huawei-iBMC-Cmdlets) — PowerShell Redfish wrapper (vendor 공식 repo)
- [iBMC Manager URI 패턴](https://manualzz.com/doc/o/1wk3dz/huawei-fusionserver-pro-2288h-v5-user-manual-9.2-logging-in-to-the-ibmc-webui)

## 9단계 절차 매핑 (rule 50 R2)

| 단계 | 작업 | 상태 |
|---|---|---|
| 1. vendor_aliases.yml 매핑 | "huawei" alias 추가 | TODO |
| 2. adapter YAML 생성 | `adapters/redfish/huawei_ibmc.yml` | TODO |
| 3. (선택) OEM tasks | `redfish-gather/tasks/vendors/huawei/` (현재 미작성) | DEFER |
| 4. vault 생성 | `vault/redfish/huawei.yml` | **SKIP (사용자 명시)** |
| 5. baseline | `schema/baseline_v1/huawei_baseline.json` | DEFER (lab 부재) |
| 6. ai-context | `.claude/ai-context/vendors/huawei.md` | TODO |
| 7. vendor-boundary-map.yaml | `.claude/policy/vendor-boundary-map.yaml` 갱신 | TODO |
| 8. live-validation | `docs/13_redfish-live-validation.md` Round 갱신 | DEFER (lab 부재) |
| 9. decision-log | `docs/19_decision-log.md` 추가 | TODO |

## 변경 (Additive)

### 1. `common/vars/vendor_aliases.yml`

```yaml
huawei:
  - "Huawei"
  - "Huawei Technologies Co., Ltd."
  - "HUAWEI"
```

### 2. `adapters/redfish/huawei_ibmc.yml` 신규

```yaml
---
# ── Origin metadata (rule 96 R1) ──────────────────────────────────────────────
# Vendor: Huawei
# Firmware: iBMC (Huawei integrated BMC)
# Origin: Huawei FusionServer Pro Series Documentation
# Last sync: 2026-05-01 (web sources only — lab 부재)
# Notes: FusionServer Pro V5 / V6. 표준 Redfish 호환 + Oem.Huawei 확장.
# Tested against: lab 부재 — web sources only
#   - source: https://support.huawei.com/enterprise/en/doc/EDOC1000163550/41dfb258/ibmc
#   - source: https://github.com/Huawei/Huawei-iBMC-Cmdlets (Huawei 공식 repo)
# 운영 사이트 도입 시: 사용자 사이트 fixture 캡처 (rule 70 / capture-site-fixture)
# Vault: 미생성 (사용자 명시 2026-05-01) — lab/사이트 도입 시 vault/redfish/huawei.yml 추가
# ──────────────────────────────────────────────────────────────────────────────
adapter_id: redfish_huawei_ibmc
channel: redfish
priority: 80
specificity: 5

match:
  vendor: ["Huawei", "HUAWEI", "Huawei Technologies Co., Ltd."]
  # firmware_patterns: 추가 정보 부족 — 일단 vendor 매칭만

capabilities:
  sections_supported:
    - system
    - hardware
    - bmc
    - cpu
    - memory
    - storage
    - network
    - firmware
    - power
    - users

collect:
  strategy: standard_only
  standard_tasks: "redfish-gather/tasks/collect_standard.yml"
  # OEM tasks 향후 작성 — 현재 standard_only 로 표준 Redfish 만 수집

normalize:
  standard: "redfish-gather/tasks/normalize_standard.yml"

metadata:
  bmc_name: "iBMC"
  manager_uri_hint: "/redfish/v1/Managers/iBMC"  # 추정 — 사이트 검증 후 확정
  oem_namespace: "Oem.Huawei"
  expected_models:
    - "FusionServer Pro 2288H V5"
    - "FusionServer Pro 2288X V5"
    - "FusionServer Pro 2488H V5"
    - "FusionServer Pro 1288H V6"
  vault_status: "not_created (user explicit 2026-05-01)"
```

### 3. `.claude/ai-context/vendors/huawei.md` 신규

```markdown
# Huawei iBMC vendor context

> 2026-05-01 신규 — 사용자 도입 의향 있음 (lab 부재). web sources only.

## BMC
- 이름: iBMC (integrated Baseboard Management Controller)
- 지원 protocol: Redfish / IPMI 2.0 / SNMP / DCMI / HTTPS
- Manager URI 추정: `/redfish/v1/Managers/iBMC` (사이트 검증 필요)
- OEM namespace: `Oem.Huawei`

## 대표 모델
- FusionServer Pro 2288H V5 (2U, dual-socket Intel Xeon Cascade Lake)
- FusionServer Pro 2288X V5 (2U, GPU 강화)
- FusionServer Pro 2488H V5 (2U, 4-socket)
- FusionServer Pro 1288H V6 (1U, V6 — Ice Lake/Sapphire Rapids 추정)

## Vault 상태

**미생성** (2026-05-01 사용자 명시). lab 또는 사이트 도입 시:
1. `vault/redfish/huawei.yml` 생성 (ansible-vault encrypt)
2. username / password 등록
3. 4단계 precheck (auth) 단계 정상 동작

현재 상태에서 호출 시: ServiceRoot 무인증 detect → vendor=huawei → vault 미존재 → errors 명시 + status=failed (gracefully).

## 회귀 검증 (lab 부재)

- 표준 Redfish 응답 mock fixture 작성 (`tests/fixtures/redfish/huawei_ibmc/`)
- Huawei-iBMC-Cmdlets GitHub repo의 sample JSON 참조
- 공식 docs의 endpoint 응답 예시 활용
- pytest 회귀: redfish_generic.yml 와의 priority 차이 확인 (huawei adapter 가 selected)

## 미해결 (사용자 결정 / lab 도입 후)

- 펌웨어 version pattern (firmware_patterns) — 사이트/lab 검증 후 추가
- model_patterns 매칭 정확성 — fixture 확보 후 보강
- OEM Oem.Huawei 필드 list — `_extract_oem_huawei()` 함수 작성 (rule 12 R1 Allowed)
- Power / Storage / Network 섹션 capabilities — 표준 endpoint 응답 형식 사이트 검증
```

### 4. `.claude/policy/vendor-boundary-map.yaml` 갱신

기존 vendor list (dell / hpe / lenovo / supermicro / cisco) 에 huawei 추가.

### 5. `docs/19_decision-log.md` 추가

```markdown
## 2026-05-01 — 신규 vendor 도입 의향 4종 (Huawei / Inspur / Fujitsu / Quanta)

### 컨텍스트
사용자 (hshwang1994) 명시: "신규 장비 도입할 의향이 있다 다만 테스트할 lab 장비가 없다. 일단 vault는 만들지 말고 코드 생성에 대한 티켓은 만들어라"

### 결정
4 vendor adapter 코드 생성 진행. vault 단계 SKIP (lab 부재).

### 영향
- adapter 신규 4종: huawei_ibmc / inspur_isbmc / fujitsu_irmc / quanta_bmc
- vendor_aliases.yml 4 entry 추가
- vendor 카운트: 5 → 9
- 운영 가능 시점: lab 또는 사이트 장비 도입 + vault 생성 시
```

## 검증

### lab 부재 상태에서 검증 가능 항목

1. **adapter_loader 점수 계산**: 신규 adapter 가 priority=80, specificity=5 로 redfish_generic (priority=0) 보다 우선 선택되는지
2. **vendor 감지**: ServiceRoot.Vendor="Huawei" mock 응답에서 huawei alias 매핑 검증
3. **mock fixture 파싱**: 공식 Huawei docs 의 sample JSON 응답으로 normalize_standard.yml 통과 검증
4. **vault 부재 graceful**: vault 없는 상태에서 호출 시 errors[] 에 "vault not found for vendor=huawei" + status=failed (4단계 precheck auth 단계)

### lab 도입 후 추가 검증

1. probe_redfish.py --vendor huawei 실측
2. baseline 생성 (schema/baseline_v1/huawei_baseline.json)
3. 사이트 fixture 캡처 (capture-site-fixture skill)
4. Round 검증 (docs/13_redfish-live-validation.md)

## Cold-start (다음 작업자)

```bash
# 1. 기존 adapter 패턴 참조
cat adapters/redfish/dell_idrac9.yml
cat adapters/redfish/lenovo_xcc.yml

# 2. vendor_aliases 갱신
$EDITOR common/vars/vendor_aliases.yml  # huawei 매핑 추가

# 3. adapter 작성
$EDITOR adapters/redfish/huawei_ibmc.yml  # 본 ticket 의 YAML 본문 사용

# 4. ai-context 작성
mkdir -p .claude/ai-context/vendors
$EDITOR .claude/ai-context/vendors/huawei.md  # 본 ticket 의 본문 사용

# 5. policy 갱신
$EDITOR .claude/policy/vendor-boundary-map.yaml  # huawei 추가

# 6. mock fixture 작성 (선택)
mkdir -p tests/fixtures/redfish/huawei_ibmc
# 공식 Huawei docs 의 sample JSON 응답 참조

# 7. 회귀
ansible-playbook --syntax-check redfish-gather/site.yml
python scripts/ai/verify_harness_consistency.py
python scripts/ai/verify_vendor_boundary.py

# 8. decision log
$EDITOR docs/19_decision-log.md
```

## 회귀 risk

- **LOW**: 신 adapter 추가 (Additive). 기존 5 vendor 매칭에 영향 없음
- **MED**: redfish_generic.yml priority=0 fallback 가 huawei vendor 응답에 매칭 안 되는 케이스 — 현재 generic 도 어느정도 cover하지만 huawei 전용이 더 정확
- **사용자 사이트 fixture 부재**: rule 25 R7-A-1 — site 실측이 spec 보다 우선. 사이트 도입 시 hotfix 가능성 (Lenovo XCC reverse regression 사례 참조)

## 관련

- audit ID: `WEB-COMPATIBILITY-AUDIT-2026-05-01.md` 의 F74
- rule: 50 R2 (vendor 추가 9단계) / 12 R1 (vendor 경계) / 96 R1-A (web sources)
- skill: add-new-vendor / capture-site-fixture / probe-redfish-vendor
